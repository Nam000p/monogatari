## Project Startup Guide

This project is fully containerized. No local installation of Java, Maven, or MySQL is required.

### 1. Prerequisite
- Install Docker Desktop.

- Ensure Docker is running.

### 2. Run the Application

Execute this command in the project root folder:

```Bash
docker-compose up --build
```